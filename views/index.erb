<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link href="https://fonts.googleapis.com/css?family=Open+Sans:300,300i,700,700i" rel="stylesheet">
  <title>WeatherTracker</title>
  <style>
    html {
      min-height: 100%;
      background-image: linear-gradient(rgba(0,0,0,.5), rgba(0,0,0,.5)), url("/views/cloudy_background.jpg");
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
    }

    form {
      padding-bottom: 3%;
    }

    ::placeholder {
      color: white;
    }

    .main {
      color: white;
      margin-top: 15%;
      background: rgba(0,0,0,0.5);
      padding: 8%;
      border-radius: 10px;
    }

    #title {
      text-align: center;
      text-transform: capitalize;
      padding-bottom: 5%;
    }

    #display-text {
      text-align: center;
    }

    #hero-input {
      border: .1rem solid orange;
      color: black;
    }

    #hero-input:focus {
      border-color: white;
    }

    #hero-button {
      margin-left: 40%;
      background-color: orange;
    }

    .animate {
      position: relative;
      animation-name: fadeUp;
      animation-duration: 2s;
    }

    @keyframes fadeUp {
      from { bottom: -350px; }
      to   { bottom: 0px; }
    }
  </style>
</head>
<body>
  <section class="container medium main">
    <div class="main-content">
      <h1 id="title">Weather Tracker</h1>
      <form action="/" method="GET">
        <input id="hero-input" name="city" type="text" placeholder="Enter a City Name" required>
        <input id="hero-button" type="submit" value="Get Weather">
      </form>

      <div class="container small secondary">
        <% if @data && @data["main"] %>
          <h1 id="display-text">It is <%= @data["main"]["temp"].round %> degrees in <%= @data["name"] %></h1>
          <p>Weather: <%= @data["weather"][0]["description"].capitalize %></p>
          <p>High: <%= @data["main"]["temp_max"].round %>°F / Low: <%= @data["main"]["temp_min"].round %>°F</p>
        <% end %>
      </div>
    </div>
  </section>
</body>
</html>
